/**
 * The glow under a lit disc — the composer's Start button and the Assistant's
 * Send. DESIGN.md §Elevation sanctions exactly these two; the string lives
 * here so they cannot drift into two hand-tuned alphas.
 */
export const LIT_DISC = "shadow-lg shadow-primary/40";
