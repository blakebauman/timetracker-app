{
	"name": "@timetracker/tsconfig",
	"version": "0.0.0",
	"private": true,
	"description": "Shared TypeScript presets: base, dom (browser / React), worker (Cloudflare Worker with JSX for React Email), library (pure TS packages).",
	"files": [
		"base.json",
		"dom.json",
		"worker.json",
		"library.json"
	]
}
